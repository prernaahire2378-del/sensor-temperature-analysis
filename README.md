# sensor-temperature-analysis
This project analyzes temperature readings from multiple weather stations using Python and the NumPy library. It calculates the data shape, computes the mean temperature for each station, filters readings above a specified threshold using boolean masking, and normalizes the dataset to a 0–1 range for better analysis.
